# Stage 3B: подтверждение переносимости LDAM-DRW между разбиениями

Статус: протокол зафиксирован до создания новых разбиений и до просмотра тестовых предсказаний.

## Научная задача

Stage 3A показал, что среди заранее заданных функций потерь только LDAM-DRW устойчиво превзошёл перекрёстную энтропию на уровне поражений: среднее изменение MCC составило `+0,0580`, 95%-й доверительный интервал `[0,0190; 0,0987]`, знак положителен для трёх из трёх начальных значений генератора случайных чисел. Однако результат получен на одном составе проверочной выборки. Stage 3B проверяет, переносится ли эффект на новые непересекающиеся по поражениям разбиения.

Основная гипотеза:

> LDAM-DRW повышает MCC на уровне поражений по сравнению с перекрёстной энтропией при изменении как состава разбиения, так и начального состояния модели.

## Основание в литературе

1. Cao et al. предложили частотно-зависимый отступ LDAM и отложенное перевзвешивание DRW. Каноническая конфигурация использует отступ, пропорциональный `n_c^(-1/4)`, и отделяет раннее обучение признаков от поздней коррекции дисбаланса. В Stage 3B сохраняются параметры Stage 3A: `max_margin=0,5`, `scale=30`, косинусный классификатор и начало DRW после 80% из 50 эпох. Источник: [*Learning Imbalanced Datasets with Label-Distribution-Aware Margin Loss*](https://proceedings.neurips.cc/paper_files/paper/2019/hash/621461af90cadfdaf0e8d4cc25129f91-Abstract.html), NeurIPS 2019.
2. Guo et al. показали, что скалярное масштабирование температуры является простым и эффективным способом последующей калибровки нейронных сетей. Температура оценивается только по проверочным предсказаниям минимизацией отрицательного логарифмического правдоподобия и затем без изменения применяется к тестовым данным. Эта ветка не участвует в выборе основного метода. Источник: [*On Calibration of Modern Neural Networks*](https://proceedings.mlr.press/v70/guo17a.html), ICML 2017, PMLR 70:1321-1330.
3. MONICA задаёт воспроизводимый контекст длиннохвостой классификации медицинских изображений. Наш протокол отличается более строгим разделением по `lesion_id` и отдельным анализом вариабельности разбиений. Источник: [*MONICA: Benchmarking on Long-tailed Medical Image Classification*](https://arxiv.org/abs/2410.02010), arXiv:2410.02010.

## Зафиксированный дизайн

### Разбиения

- начальные значения построения разбиений: `101`, `202`, `303`;
- единица разделения: `lesion_id`, а при его отсутствии уникальное изображение;
- в каждом классе: 100 тестовых, 50 проверочных изображений и длиннохвостая обучающая выборка `[5000, 2590, 1342, 695, 360, 187, 97, 51]`;
- пересечения поражений между обучением, проверкой и тестом должны быть нулевыми;
- контрольные суммы всех описаний выборок сохраняются в `data_splits/stage3b/split_registry.json`.

### Матрица обучения

| Фактор | Значения |
|---|---|
| Метод | перекрёстная энтропия; LDAM-DRW |
| Начальное значение разбиения | 101; 202; 303 |
| Начальное значение модели | 42; 43; 44 |
| Число обучений | 18 |
| Архитектура | ResNet-50, ImageNet-1K V2 |
| Эпохи | 50 без ранней остановки |
| Пакет | 256, один шаг накопления |
| Оптимизатор | Adam, скорость обучения `3e-4`, без уменьшения весов |
| Формат | bfloat16, channels-last |
| Основная контрольная точка | `last.pt`, эпоха 50 |
| Дополнительная | `best.pt` по MCC на уровне поражений |

Из каждого из девяти запусков с перекрёстной энтропией рассчитывается коррекция логитов с `tau=1`. Для каждого из девяти запусков LDAM-DRW отдельно оценивается скалярная температура по проверочной выборке. Производные расчёты выполняются параллельно и не требуют обучения.

## Защита тестовой выборки

Обучающие конфигурации не содержат `test_csv`, а `run_test=false`. После 18 обучений проверяются:

- единый коммит кода;
- уникальная сигнатура каждого запуска;
- контрольные суммы обучающей и проверочной выборок;
- 50 завершённых эпох;
- политика `last.pt` как основная;
- наличие всех структурированных артефактов;
- отсутствие тестовых предсказаний;
- наличие девяти коррекций логитов и девяти температур, рассчитанных только по проверке.

Тест открывается только при `readiness.ready=true` и только один раз. Файл `TEST_EVALUATION_STARTED.json` запрещено удалять или обходить.

## Метрики и критерий подтверждения

Основная величина: `MCC_LDAM - MCC_CE` на уровне поражений для `last.pt`.

Дополнительные показатели: сбалансированная точность, макроусреднённые F1/AUROC/AUPRC, худшая полнота класса, показатели MEL/AK/SCC, ECE, отрицательное логарифмическое правдоподобие, оценка Брайера, показатели на уровне изображений и вычислительная стоимость.

Подтверждение требует одновременно:

1. нижняя граница объединённого иерархического 95%-го доверительного интервала изменения MCC выше нуля;
2. среднее изменение MCC положительно не менее чем для двух из трёх разбиений;
3. отсутствует устойчивое клинически значимое падение полноты MEL или SCC;
4. результат не определяется одним начальным значением модели.

Иерархическая повторная выборка включает уровни `split seed -> model seed -> lesion_id` и не менее 10 000 повторов. Анализ `best.pt` и температурной калибровки является дополнительным и не заменяет основной результат.

## Вычислительная организация

Одновременное обучение нескольких моделей на одной RTX 5080 не применяется: оно меняет конкуренцию за память и вычислительные блоки и ухудшает сопоставимость. GPU-обучения выполняются последовательно; ожидаемая загрузка GPU близка к 100%, пиковая память около 11 ГиБ. Загрузка изображений использует 12 процессов, закреплённую память, четыре предварительно загруженных пакета на процесс и 24 ГиБ общей памяти контейнера. Построение трёх разбиений, проверка артефактов, калибровка и статистический анализ распараллеливаются по независимым заданиям.

Оценка длительности: около 3,5 часа обучения и до 30 минут на производные расчёты, одноразовую проверку теста и 10 000 повторов статистического анализа.

## Фактические результаты

Все 18 обучений завершены на одном коммите `945773b`. Созданы 9 результатов коррекции логитов и 9 результатов температурной калибровки. Шлюз готовности принял ровно 18 обучений и 18 производных расчётов без ошибок. Закрытая тестовая выборка была открыта один раз; создан необратимый маркер `outputs/stage3b_locked_test/TEST_EVALUATION_STARTED.json`.

### Основное сравнение на уровне поражений

Показатели ниже усреднены по девяти сочетаниям трёх разбиений и трёх начальных состояний модели. Основной результат относится к `last.pt`, эпоха 50.

| Метод | MCC | Сбалансированная точность | Макро-F1 | Макро-AUPRC | Макро-AUROC | ECE | NLL | Оценка Брайера |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Перекрёстная энтропия | 0,4475 | 0,4770 | 0,4593 | 0,5819 | 0,8744 | 0,3367 | 2,8473 | 0,8011 |
| LDAM-DRW, исходные вероятности | 0,5213 | 0,5546 | 0,5468 | 0,5963 | 0,8715 | 0,2885 | 3,3814 | 0,7082 |
| Коррекция логитов `tau=1` | 0,4966 | 0,5261 | 0,5145 | 0,6021 | 0,8850 | 0,2785 | 2,0814 | 0,6964 |
| LDAM-DRW после температурной калибровки | **0,5262** | **0,5581** | **0,5507** | **0,6167** | **0,8885** | **0,0969** | **1,3102** | **0,5738** |

Для исходного LDAM-DRW относительно перекрёстной энтропии:

- среднее изменение MCC: `+0,0738`, знак положителен в `9/9` пар;
- среднее изменение сбалансированной точности: `+0,0777`, знак положителен в `9/9` пар;
- среднее изменение макро-F1: `+0,0875`, знак положителен в `9/9` пар;
- среднее изменение макро-AUPRC: только `+0,0144`, знак положителен в `4/9` пар;
- среднее изменение макро-AUROC: `-0,0028`, знак положителен в `4/9` пар;
- средняя худшая полнота класса выросла с `0,1010` до `0,2369`.

Таким образом, подтверждён сильный эффект на решения при фиксированном правиле `argmax`, но не универсальное улучшение качества ранжирования.

### Иерархическая статистическая оценка

Повторная выборка выполнена на уровнях `начальное значение разбиения -> начальное значение модели -> lesion_id`, 10 000 повторов. Перед повторным анализом сохранённые вероятности явно нормированы; максимальное исходное отклонение суммы строки от единицы составляло лишь `1,93e-7` и не влияло на предсказанный класс.

| Изменение LDAM-DRW минус CE | Среднее | 95%-й ДИ | Вероятность положительного эффекта |
|---|---:|---:|---:|
| MCC на уровне поражений | **+0,0737** | **[+0,0329; +0,1173]** | 1,000 |
| Сбалансированная точность | **+0,0774** | **[+0,0433; +0,1135]** | 1,000 |

Среднее изменение MCC было положительным во всех разбиениях:

| Начальное значение разбиения | Среднее изменение MCC | Отдельные изменения по моделям |
|---:|---:|---|
| 101 | +0,0944 | +0,0296; +0,1817; +0,0718 |
| 202 | +0,0454 | +0,0096; +0,0369; +0,0896 |
| 303 | +0,0815 | +0,0450; +0,0855; +0,1141 |

Все четыре заранее заданных критерия подтверждения выполнены: доверительный интервал выше нуля, эффект положителен на трёх из трёх разбиений, полнота MEL/SCC не показывает устойчивого падения, результат положителен для всех девяти начальных состояний моделей.

### Клинически важные классы

| Класс | Показатель | CE | LDAM-DRW | Изменение |
|---|---|---:|---:|---:|
| MEL | полнота | 0,4484 | 0,4924 | +0,0440 |
| MEL | F1 | 0,3406 | 0,4231 | +0,0825 |
| MEL | AUPRC | 0,3356 | 0,3955 | +0,0599 |
| AK | полнота | 0,3115 | 0,4500 | +0,1386 |
| AK | F1 | 0,3866 | 0,4679 | +0,0814 |
| AK | AUPRC | 0,4654 | 0,4602 | -0,0052 |
| SCC | полнота | 0,2075 | 0,3389 | +0,1314 |
| SCC | F1 | 0,2785 | 0,3900 | +0,1114 |
| SCC | AUPRC | 0,4322 | 0,4027 | -0,0295 |

Для MEL LDAM улучшил также чувствительность при фиксированной специфичности 90% на `+0,0847` и при 95% на `+0,0658`. Для SCC наблюдается противоположный эффект: чувствительность при специфичности 90% снизилась на `-0,0701`, а при 95% на `-0,0641`. Следовательно, повышение обычной полноты SCC вызвано смещением рабочей точки и не означает улучшения всей кривой ранжирования.

### Калибровка и дополнительный анализ

Температура, оценённая отдельно для каждого запуска LDAM только по проверочной выборке, находилась в диапазоне `[5,63; 7,01]`, среднее `6,35`. Это подтверждает выраженную избыточную уверенность исходных логитов LDAM.

Температурная калибровка относительно исходного LDAM:

- уменьшила ECE в среднем на `0,1916` во всех `9/9` запусках;
- уменьшила NLL на `2,0712` во всех `9/9` запусках;
- уменьшила оценку Брайера на `0,1344` во всех `9/9` запусках;
- увеличила макро-AUPRC на `0,0204` во всех `9/9` запусках;
- практически не изменила решения отдельных изображений, но могла изменить решение поражения после усреднения вероятностей нескольких снимков.

Коррекция логитов без повторного обучения также была сильным контролем: относительно CE она улучшила MCC на `+0,0490` и макро-AUPRC на `+0,0202`, оба знака положительны в `9/9` пар. Поэтому последующая коррекция априорных вероятностей является обязательным простым сравнением для будущей статьи.

Дополнительный анализ `best.pt` был слабее основного: средний MCC составил `0,4644` для CE и `0,4867` для LDAM-DRW. Это подтверждает заранее принятое решение использовать фиксированную 50-ю эпоху как основную и показывает, что преимущество LDAM связано с поздней фазой DRW, а не со случайным ранним максимумом.

### Вычислительная стоимость

Среднее время одного запуска составило 696,9 секунды для CE и 697,7 секунды для LDAM-DRW. Пиковое использование видеопамяти одинаково: 11,04 ГиБ. Следовательно, подтверждённое улучшение LDAM не требует заметного дополнительного времени обучения или памяти относительно CE. Средняя лучшая эпоха равна 34,9 для CE и 41,9 для LDAM-DRW.

## Научный вывод и решение по Stage 3C

Stage 3B подтверждает переносимое преимущество полного пакета LDAM-DRW для MCC, сбалансированной точности, макро-F1 и полноты редких классов. Одновременно он опровергает более сильное утверждение, что LDAM сам по себе улучшает ранжирование всех редких диагнозов: макро-AUROC не вырос, а SCC AUPRC и чувствительность при высокой специфичности снизились.

Следующий этап должен быть не синтетической аугментацией, а компонентной абляцией на тех же трёх разбиениях без повторного открытия текущего теста:

1. CE с линейным классификатором — существующий контроль;
2. CE с косинусным классификатором;
3. LDAM без DRW;
4. CE с DRW;
5. полный LDAM-DRW — существующий контроль.

Нужно обучить только три отсутствующих варианта для трёх разбиений и трёх начальных состояний, то есть 27 новых запусков; CE и полный LDAM-DRW переиспользуются как зафиксированные опорные результаты. Основная цель Stage 3C — отделить вклад геометрии классификатора, частотно-зависимого отступа и отложенного перевзвешивания. Синтетические данные следует вводить только после выбора механизма, который улучшает не только рабочую точку, но и AUPRC либо чувствительность при высокой специфичности.

## Ограничения

- Эффект относится к полному пакету `косинусный классификатор + LDAM + DRW`; вклад компонентов будет проверяться отдельной абляцией только после подтверждения.
- Проверяются три разбиения и три начальных состояния модели, что лучше одного разбиения, но не заменяет внешнюю проверку.
- Температура использует ту же проверочную выборку, что и контроль контрольной точки; поэтому калибровочные результаты трактуются как дополнительные.
- Синтетические данные до завершения Stage 3B не используются.

## Журнал выполнения

- `2026-08-02 19:56`: первая попытка запуска одноразовой оценки остановилась до проверки готовности и до создания `TEST_EVALUATION_STARTED.json`, поскольку серверное виртуальное окружение вызывается только внутри квалифицированного контейнера. Научные артефакты и закрытый тест не были затронуты. Оболочка запуска исправлена: readiness, GPU-inference и анализ теперь выполняются в `local/research-cuda-notebook:latest` с тем же зафиксированным идентификатором кода обучения `945773b`. Добавлена регрессионная проверка контейнерного запуска.
